# GEMINI.md — Antigravity Entry Point for StillDone

You are the coding executor for StillDone.

Before doing anything:
1. Read `AGENTS.md`.
2. Read `plans/STILLDONE_MASTER_EXECUTION_PLAN.md`.
3. Read `docs/HANDOFF.md`.
4. Inspect canonical remote `main` when it exists.
5. Work on exactly the active micro-task.

Critical StillDone laws:
- execute success is not verification;
- required mutation must be independently read back;
- model prose cannot promote evidence state;
- `NOT_RUN` is never `PASS`;
- `READY` can later become `DRIFTED`;
- no silent live→fixture fallback;
- zero personal spend;
- no secrets in repository/evidence;
- donors are concept-only unless provenance explicitly authorizes reuse;
- local commit is not remote closure.

The operator is non-expert. External setup instructions must be in Turkish, screen-by-screen, and must state billing/card risk.

Do not begin future phases because they appear adjacent or convenient.

At completion, use the report contract in `AGENTS.md` and do not self-award independent QA PASS.
